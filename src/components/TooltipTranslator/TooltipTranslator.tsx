import { Paper } from "@mantine/core"

export const TooltipTranslator = () => {
    return (
        <Paper>
            <p className="text-gray-700">
                Hover over this text to see the tooltip.
            </p>
        </Paper>
    )
}